<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- State pressed -->
    <item android:state_pressed="true" android:color="@color/ic_pressed_color" />

    <!-- State selected -->
    <item android:state_selected="true" android:color="@color/ic_active_color" />

    <!-- Default state -->
    <item android:color="@color/ic_inactive_color" />
</selector>
